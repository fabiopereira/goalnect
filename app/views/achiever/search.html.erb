<h1>Achievers</h1>
<h3>Result from your search '<%= params[:q] %>'</h3>
<% @achievers.each do |achiever| %>
<div>            
	<a href="<%= achiever.full_url %>">
	<%= user_image(achiever, :thumb) %>
	<%= achiever.screen_name %>
	</a>
</div>
<% end %>
