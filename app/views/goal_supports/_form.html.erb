<%= form_for(@goal_support) do |f| %>
  <% if @goal_support.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@goal_support.errors.count, "error") %> prohibited this goal_support from being saved:</h2>

      <ul>
      <% @goal_support.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :i_support %><br />
    <%= f.check_box :i_support %>
  </div>
  <div class="field">
    <%= f.label :goal_id %><br />
    <%= f.number_field :goal_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
