
<div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <button type="button" class="btn btn-navbar collapsed" data-toggle="collapse" data-target=".nav-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="brand" href="/"><%= image_tag("goalnect.logo.png", id: "logo", alt: "Goalnect", width: "135") %></a>
          <div class="nav-collapse collapse" style="height: 0px;">
            <ul class="nav pull-right">
	            <li>
					<%= link_to t("static.howitworks"), "/static/how-it-works-#{I18n.locale}" %>
		        </li>
				<li><a href="http://www.facebook.com/goalnect" target="blank"><%= image_tag("facebook.png", width: "25") %></a></li>
				<li><a href="http://www.twitter.com/goalnect" target="blank"><%= image_tag("twitter.png", width: "25") %></a></li>
				<li><a href="mailto:team@goalnect.com" target="blank"><%= image_tag("goalnect-mailto-48x48.png", width: "25") %></a></li>
				<% if admin_signed_in? %>
				    <li>
						<%= link_to "Logout Admin", destroy_admin_session_path, :method => :delete %>
					</li>
				<% end %>
				<% Togg.le(:feature_login) do %>
				<li>
				<form action="/search">
				  <input id="q" name="q" type="text" placeholder="<%= t 'search_friends' %>"/>
				  <input type="submit" value="Search"/>                
				</form>
				</li>
				<% if user_signed_in? %>
				 <input type="hidden" id="current_user_test" value="<%=current_user.username %>"/>
				  <li>
					<% if session[:fb_token].nil? %>
					   <%= link_to "Logout", destroy_user_session_path, :method => :delete %>
					<% else %>
					   <%= link_to "Logout", facebook_logout_path %>
					<% end %>      
				  </li>
				<% else %>
				  <li>
				    <%= link_to('Login', new_user_session_path)  %>
				  </li>
				<% end %>
				<% end %>
            </ul>
          </div>
        </div>
      </div>
    </div>
</div>
<br/>