	<% if target.errors.any? %>
  	<div id="error_explanation">
    <h2>I'm sorry, please correct the'<%= pluralize(target.errors.count, "error") %> below:</h2>
    <ul>
    <% target.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
