<%= form_for(@goal_option) do |f| %>
  <% if @goal_option.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@goal_option.errors.count, "error") %> prohibited this goal_option from being saved:</h2>

      <ul>
      <% @goal_option.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :is_system %><br />
    <%= f.check_box :is_system %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
